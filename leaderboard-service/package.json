{
  "name": "tetrisgame-leaderboard",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local-protocol https --compatibility-date 2026-09-03",
    "deploy": "wrangler deploy",
    "test": "wrangler deploy --dry-run --outdir .test-build && node --test test/*.test.mjs",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "@types/node": "^26.4.1",
    "miniflare": "^5.20260903.0-alpha",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
